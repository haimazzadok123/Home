// כיול מול ה-DOM האמיתי של דף ההזמנות.
//
// לא ניתן היה לגלוש בפועל אל parks.org.il מסביבת הפיתוח (חסימת רשת ברמת
// מדיניות הארגון), ולכן השדות כאן ריקים בכוונה. כדי להשלים בדיקת זמינות
// מדויקת פר-תאריך:
//
//   1. הריצו את הסוכן פעם אחת (לוקאלית או ב-GitHub Actions).
//   2. פתחו את צילום המסך / קובץ ה-HTML שנשמרים בתיקיית debug/.
//   3. מצאו בכלי הפיתוח של הדפדפן (F12) את הסלקטורים המתאימים ומלאו כאן.
//
// כל עוד השדות ריקים, הסוכן ירוץ ב"מצב כללי" בלבד: הוא ידווח על מצב הדף
// כפי שהוא, ללא יכולת לבחור תאריכים ספציפיים או לזהות זמינות פר-סוף-שבוע.
export const selectors = {
  // סלקטור ללחיצה שפותחת את תהליך ההזמנה / לוח הזמינות, אם קיים דף ביניים.
  openBookingSelector: null,

  // סלקטורים לשדות תאריך כניסה/יציאה (למשל: 'input[name="checkin"]').
  checkInSelector: null,
  checkOutSelector: null,

  // סלקטור לכפתור החיפוש/בדיקה לאחר מילוי תאריכים.
  searchButtonSelector: null,

  // מחרוזות טקסט המעידות על זמינות / אזל המלאי, לזיהוי לפי תוכן הדף.
  availableTextHints: ["הזמן עכשיו", "הוסף להזמנה", "המשך להזמנה"],
  soldOutTextHints: [
    "אין תאריכים פנויים",
    "אין מקום פנוי",
    "אזל המלאי",
    "מלא",
    "לא ניתן להזמין",
  ],
};

export const isCalibrated = Boolean(
  selectors.checkInSelector && selectors.checkOutSelector,
);
